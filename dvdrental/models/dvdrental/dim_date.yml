version: 2

models:
  - name: dim_date
    columns:
      - name: date_key
        description: The surrogate key of the dim_date table 
        tests:
          - unique
          - not_null

      - name: date
        description: The natural key of the payment table 
        tests:
          - not_null
          - unique